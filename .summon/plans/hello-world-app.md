---
status: pending
title: Minimal Hello World App
---

Project is currently empty (only README.md and env.example), so the full app scaffold must be created.

1. Create `package.json` declaring an ESM project with scripts for dev, build and preview, and dependencies: react, react-dom, @tanstack/react-router; dev dependencies: vite, @vitejs/plugin-react, typescript, @types/react, @types/react-dom, tailwindcss, @tailwindcss/vite, @tanstack/router-plugin. Outcome: npm install produces a working toolchain.
2. Create `tsconfig.json` (and `tsconfig.node.json` if needed for the Vite config) with strict mode, bundler module resolution, JSX react-jsx, and a `@/*` path alias mapped to `src/*`. Outcome: absolute imports like `@/components/X` type-check.
3. Create `vite.config.ts` registering the React plugin, the Tailwind CSS v4 Vite plugin, and the TanStack Router plugin configured for file-based routes in `src/routes`, plus a resolve alias for `@` → `src`. Outcome: routes are auto-discovered and `src/routeTree.gen.ts` is generated on dev/build.
4. Create `index.html` at the project root with a `#root` div and a module script referencing `src/main.tsx`. Outcome: Vite has an entry document.
5. Create `src/styles/global.css` whose first line is exactly `@import "tailwindcss";`. Outcome: Tailwind utilities are available app-wide.
6. Create `src/main.tsx` that imports `src/styles/global.css` once, builds the router from the generated route tree, registers the router type, and renders `RouterProvider` into `#root` inside StrictMode. Outcome: app boots with routing and styles.
7. Create `src/routes/__root.tsx` exporting the root route with a minimal shell: a full-height wrapper element rendering `<Outlet />` and nothing else (no nav, no chrome). Outcome: pages fill the viewport.
8. Create `src/routes/index.tsx` for the `/` route rendering a single section that fills the screen height, centers its content both vertically and horizontally with flexbox, uses a subtle light background (e.g. a soft neutral/slate tint with a dark-mode-friendly counterpart), and shows one large, bold, tight-tracking "Hello World" heading with balanced typography. No other content. Outcome: visiting `/` shows a cleanly centered Hello World heading.
9. Create `.gitignore` covering `node_modules`, `dist`, `.env`, and local editor files; leave `src/routeTree.gen.ts` generated by the plugin and never hand-edited. Outcome: repo stays clean.
10. Verify by running the dev server and loading `/`: heading is centered on both axes, background tint visible, no console or type errors. Outcome: minimal Hello World app confirmed working.
